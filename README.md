# Real-time Cellular Forms by Andy Lomas in PyOpenGL with SSAO

## How to run
1. Make sure you have Python 3.9+ installed.
2. Install the required packages:
   ```bash
   pip install -r requirements.txt
   ```
3. Run from main script:
   ```bash
   python main.py
   ```